// Randomized delay used after CMP/iframe events to simulate human-like interaction.
// Produces a 400-600ms range: HUMAN_LIKE_DELAY_BASE_MS + random[0, VARIANCE).
export const HUMAN_LIKE_DELAY_BASE_MS = 400;
export const HUMAN_LIKE_DELAY_VARIANCE_MS = 200;
